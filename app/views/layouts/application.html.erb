<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link href='http://fonts.googleapis.com/css?family=Questrial|Alegreya+Sans+SC|Michroma|Quicksand|Josefin+Sans|Muli|Maven+Pro|Karla|PT+Sans+Caption|Julius+Sans+One|Carrois+Gothic+SC|Numans' rel='stylesheet' type='text/css'>
    <title><%= content_for?(:title) ? yield(:title) : "Protest Australia" %></title>

    <%= stylesheet_link_tag "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
  </head>

  <body>
     <div class="container">
         <header class="header">
            <h2 class="top_header"><%= link_to "Protest Australia", root_path %></h1>
            <div class="top-menu">
               <div class="button-small search-icon" id="search-icon"><i class="fa fa-search"></i>
                  <div id="search-bar" class="search-bar">
                     <%= render 'common/search_field' %>
                  </div>
               </div>
               <div class="button-small"><i class="fa fa-twitter"></i></div>
               <div class="button-small"><i class="fa fa-facebook"></i></div>
               <% if signed_in? %>
                     <% if current_user.try(:admin?) %>
                        <div class="button-small"><i class="fa fa-star med-icon yellow-text"></i></div>
                     <% end %>
                     <div class="user-email"><%= current_user.email %></div>
                     <%= link_to "My Protests", my_protests_path, class: "button" %> <%= link_to "Sign Out", destroy_user_session_path, :method => :delete, class: "button" %>
               <% else %>
                     <%= link_to "Register", new_user_registration_path, :method => :get, class: "button" %> <%= link_to "Sign In", new_user_session_path, :method => :get, class: "button" %>
               <% end %>

               <div class="button-small menu-top-right" id="menu-icon"><i class="fa fa-bars"></i>

                  <ul class="dropdown-menu" id="dropdown-menu">
                     <li class="dropdown-menu-item"><%= link_to "Protests", protests_path %></li>
                     <li class="dropdown-menu-item"><%= link_to "Add a Protest", new_path %></li>
                     <li class="dropdown-menu-item"><%= link_to "My Protests", my_protests_path %></li>
                     <li class="dropdown-menu-item"><%= link_to "Related Sites", related_sites_path %></li>
                     <li class="dropdown-menu-item"><%= link_to "About Us", about_us_path %></li>
                     <li class="dropdown-menu-item"><%= link_to "Contact Us", contact_us_path %></li>
                     <% if current_user.try(:admin?) %>
                        <li class="dropdown-menu-item"><%= link_to "Admin Panel", admin_path %></li>
                     <% end %>
                  </ul><!-- dropdown-menu -->
               </div><!-- id:menu-icon -->
         </header>
         <div class="main-body">
   			<% flash.each do |key, value| %>
   				<div id="message"><p><%= value %></p></div>
   			<% end %>

   		    <%= yield %>
          </div><!-- main-body -->
       </div><!-- container -->
       <div class="clear"></div>
       <%= render 'common/footer' %>

   	<%= javascript_include_tag "application" %>
  </body>
</html>
